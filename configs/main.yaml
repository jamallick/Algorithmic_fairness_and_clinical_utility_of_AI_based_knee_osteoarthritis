seed: 1701
model: densenet
num_classes: 5
image_size: 224
batch_size: 32
epochs: 100
learning_rate: 0.0001
weight_decay: 0.0001
optimizer: adamw
scheduler: cosine
warmup_epochs: 5
grad_clip_norm: 1.0
precision: fp32
workers: 8
bootstrap_resamples: 1000
permutation_resamples: 10000
calibration_bins: 10
minimum_cell_size: 30
test_fraction: 0.2
validation_fraction: 0.1
paper_training_parameters_reported: false

